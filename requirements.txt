setuptools>=68.0.0

# HTTP / environment
requests>=2.31.0
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0

# Data loading (A1)
pandas>=2.0.0

# NLP utilities (A2)
langdetect>=1.0.9

# LangGraph / LangChain (pipeline orchestration)
langgraph>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0

# FastAPI layer
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# YouTube Data API v3 + transcription (A0 Collector — PRD v1.1)
google-api-python-client>=2.0
youtube-transcript-api>=0.6

# Progress bars (optional, useful in notebooks)
tqdm>=4.66.0

# HuggingFace backend (Kaggle GPU / offline — optional)
# Uncomment on Kaggle or when using LLM_BACKEND=huggingface
# torch>=2.0.0
# transformers>=4.40.0
# accelerate>=0.29.0

# Dev / linting (optional)
# pytest>=8.0.0
# ruff>=0.6.0
# mypy>=1.10.0
